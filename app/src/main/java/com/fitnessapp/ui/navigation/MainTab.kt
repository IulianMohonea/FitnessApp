package com.fitnessapp.ui.navigation

enum class MainTab(
    val label: String,
    val shortLabel: String
) {
    Statistics(
        label = "Statistics",
        shortLabel = "Stats"
    ),
    Home(
        label = "Home",
        shortLabel = "Home"
    ),
    History(
        label = "History",
        shortLabel = "Hist"
    )
}
